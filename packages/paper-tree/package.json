{
  "name": "paper-tree",
  "version": "0.2.0",
  "description": "",
  "main": "./src/paper-tree.js",
  "types": "./dist/paper-tree.d.ts",
  "type": "module",
  "scripts": {
    "test": "vitest run --coverage",
    "type-check": "tsc --noEmit",
    "build": "rimraf dist && tsc --project tsconfig-dec.json",
    "clean": "rimraf dist && rimraf coverage && rimraf node_modules"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "paper-tree-ambient-types": "*",
    "@types/node": "^18.15.11",
    "@vitest/coverage-c8": "^0.30.0",
    "rimraf": "^4.4.1",
    "typescript": "^5.0.3",
    "vitest": "^0.30.0"
  }
}
